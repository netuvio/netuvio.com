export type MessageSchema = {
    common: {
        comingSoon: string,
    },

    home: {
        title: string,
            // hlavni text je primo v hero sekci komponente, ne zde
        subtitle: string,
        learnMore: string,
    }
}
